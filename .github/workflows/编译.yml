name: Build All Platforms

on:
  push:
    tags:
      - 'v*'  # 当推送 v1.0.0 这样的 tag 时触发
  workflow_dispatch:  # 也支持手动触发

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    permissions:
      contents: read
      packages: write
    timeout-minutes: 30
    
    strategy:
      matrix:
        include:
          # Windows x64
          - os: windows-latest
            platform: windows
            arch: amd64
            ext: .exe
            compiler: ""
            static: false
            
          # Linux x64
          - os: ubuntu-latest
            platform: linux
            arch: amd64
            ext: ''
            compiler: ""
            static: false
            
          # Linux ARM64 (glibc)
          - os: ubuntu-24.04-arm
            platform: linux
            arch: arm64
            ext: ''
            compiler: ""
            static: false
            
          # macOS Intel
          - os: macos-13
            platform: macos
            arch: amd64
            ext: ''
            compiler: ""
            static: false
            
          # macOS Apple Silicon
          - os: macos-14
            platform: macos
            arch: arm64
            ext: ''
            compiler: ""
            static: false
            
          # OpenWrt aarch64 (arm64-musl，静态链接)
          - os: ubuntu-latest
            platform: linux
            arch: arm64-musl
            ext: ''
            compiler: aarch64-linux-musl-gcc
            static: true

    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 设置 Python 环境
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: 安装依赖（通用）
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller
          pip install --upgrade certifi urllib3 requests

      # 优化 macOS 构建依赖
      - name: 优化 macOS 构建依赖
        if: matrix.platform == 'macos'
        run: |
          brew install zlib openssl
          echo "LDFLAGS=-L/usr/local/opt/zlib/lib -L/usr/local/opt/openssl@3/lib" >> $GITHUB_ENV
          echo "CPPFLAGS=-I/usr/local/opt/zlib/include -I/usr/local/opt/openssl@3/include" >> $GITHUB_ENV

      # 修复：Git 克隆时拉取标签，而非指定不存在的分支
      - name: 安装 arm64-musl 交叉编译环境（OpenWrt 专用）
        if: matrix.arch == 'arm64-musl'
        run: |
          # 1. 安装最小化编译依赖
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends make git curl gcc g++ patch bison flex texinfo

          # 2. 修复：浅克隆 + 拉取标签（v1.2.4 是标签，不是分支）
          git clone --depth 1 --tags https://github.com/richfelker/musl-cross-make.git
          cd musl-cross-make
          # 切换到 v1.2.4 标签（确保版本稳定）
          git checkout tags/v1.2.4

          # 3. 关键配置：禁用测试用例、文档、C++ 等非必需组件
          cat > config.mak << EOF
          TARGET = aarch64-linux-musl
          OUTPUT = /usr/local/musl-cross
          CONFIG_NOCXX = 1  # 禁用 C++ 编译器
          CONFIG_NO_TESTS = 1  # 禁用测试套件（核心优化）
          CONFIG_NODOC = 1  # 禁用文档
          CONFIG_NO_FORTRAN = 1  # 禁用 Fortran
          CONFIG_NO_ATOMIC = 1  # 禁用 atomic
          CFLAGS = -O2  # 编译优化
          CXXFLAGS = -O2
          MAKEFLAGS = -j2  # 限制并行线程
          EOF

          # 4. 编译并安装
          make
          sudo make install

          # 5. 添加编译器路径到环境变量
          echo "/usr/local/musl-cross/bin" >> $GITHUB_PATH

          # 6. 验证编译器
          aarch64-linux-musl-gcc --version

      # 优化 Python 编译：禁用不必要的模块
      - name: 构建静态链接的 Python（arm64-musl）
        if: matrix.arch == 'arm64-musl'
        run: |
          # 安装 Python 最小化依赖
          sudo apt-get install -y --no-install-recommends zlib1g-dev libssl-dev libffi-dev

          # 下载 Python 3.10 源码（国内镜像）
          wget -c https://mirrors.tuna.tsinghua.edu.cn/python/3.10.13/Python-3.10.13.tgz -O Python-3.10.13.tgz
          tar -xzf Python-3.10.13.tgz
          cd Python-3.10.13
          
          # 配置 Python 静态编译
          ./configure \
            --prefix=/usr/local/python3.10-musl-arm64 \
            --build=x86_64-linux-gnu \
            --host=aarch64-linux-musl \
            --target=aarch64-linux-musl \
            --enable-optimizations \
            --enable-shared=no \
            --enable-static=yes \
            --with-ssl=/usr/local/musl-cross/aarch64-linux-musl \
            --with-zlib=/usr/lib/x86_64-linux-gnu \
            --without-pymalloc \
            --without-doc-strings \
            --disable-test-modules \
            --disable-ipv6 \
            --disable-shared \
            --disable-lib2to3 \
            --disable-tk \
            --disable-nis \
            --disable-gdbm \
            --disable-sqlite3 \
            CC=aarch64-linux-musl-gcc \
            CXX=aarch64-linux-musl-g++ \
            AR=aarch64-linux-musl-ar \
            RANLIB=aarch64-linux-musl-ranlib

          # 编译 Python
          make -j2
          sudo make install
          
          # 切换到静态 Python
          export PATH=/usr/local/python3.10-musl-arm64/bin:$PATH
          python3 --version
          
          # 安装依赖
          python3 -m pip install --upgrade pip
          pip3 install -r requirements.txt
          pip3 install pyinstaller
          pip3 install --upgrade certifi urllib3 requests

      # Windows 构建
      - name: 构建可执行文件 (Windows)
        if: matrix.platform == 'windows'
        run: |
          python -m PyInstaller --onefile --name CloudflareSpeedTest-${{ matrix.platform }}-${{ matrix.arch }} --clean --console --hidden-import requests --hidden-import urllib3 --hidden-import certifi --hidden-import charset_normalizer --hidden-import idna --exclude-module tkinter --exclude-module matplotlib --exclude-module numpy --exclude-module pandas --exclude-module PIL --exclude-module cv2 --exclude-module scipy --strip --optimize 2 cloudflare_speedtest.py

      # Linux 构建
      - name: 构建可执行文件 (Linux)
        if: matrix.platform == 'linux' && matrix.arch != 'arm64-musl'
        run: |
          python -m PyInstaller --onefile --name CloudflareSpeedTest-${{ matrix.platform }}-${{ matrix.arch }} --clean --console --hidden-import requests --hidden-import urllib3 --hidden-import certifi --hidden-import charset_normalizer --hidden-import idna --exclude-module tkinter --exclude-module matplotlib --exclude-module numpy --exclude-module pandas --exclude-module PIL --exclude-module cv2 --exclude-module scipy --strip --optimize 2 cloudflare_speedtest.py

      # macOS 构建
      - name: 构建可执行文件 (macOS)
        if: matrix.platform == 'macos'
        env:
          PYINSTALLER_NO_SIGN: 1
        run: |
          python -m PyInstaller --onefile --name CloudflareSpeedTest-${{ matrix.platform }}-${{ matrix.arch }} --clean --console --hidden-import requests --hidden-import urllib3 --hidden-import certifi --hidden-import charset_normalizer --hidden-import idna --exclude-module tkinter --exclude-module matplotlib --exclude-module numpy --exclude-module pandas --exclude-module PIL --exclude-module cv2 --exclude-module scipy --optimize 2 --osx-bundle-identifier com.example.cloudflare-speedtest cloudflare_speedtest.py

      # OpenWrt arm64-musl 构建
      - name: 构建可执行文件 (OpenWrt arm64-musl)
        if: matrix.arch == 'arm64-musl'
        run: |
          export PATH=/usr/local/python3.10-musl-arm64/bin:$PATH
          python3 -m PyInstaller \
            --onefile \
            --name CloudflareSpeedTest-${{ matrix.platform }}-${{ matrix.arch }} \
            --clean \
            --console \
            --hidden-import requests \
            --hidden-import urllib3 \
            --hidden-import certifi \
            --hidden-import charset_normalizer \
            --hidden-import idna \
            --exclude-module tkinter \
            --exclude-module matplotlib \
            --exclude-module numpy \
            --exclude-module pandas \
            --exclude-module PIL \
            --exclude-module cv2 \
            --exclude-module scipy \
            --strip \
            --optimize 2 \
            --add-binary "/usr/local/musl-cross/aarch64-linux-musl/lib/libc.so:." \
            --add-binary "/usr/local/musl-cross/aarch64-linux-musl/lib/libz.so:." \
            --add-binary "/usr/local/musl-cross/aarch64-linux-musl/lib/libssl.so:." \
            --add-binary "/usr/local/musl-cross/aarch64-linux-musl/lib/libcrypto.so:." \
            --runtime-tmpdir /tmp \
            cloudflare_speedtest.py

      # Windows 重命名
      - name: 重命名文件（Windows）
        if: matrix.platform == 'windows'
        run: |
          move dist\CloudflareSpeedTest-${{ matrix.platform }}-${{ matrix.arch }}.exe dist\CloudflareSpeedTest-${{ matrix.platform }}-${{ matrix.arch }}${{ matrix.ext }}

      # 赋予执行权限
      - name: 赋予执行权限（Linux/macOS + OpenWrt）
        if: matrix.platform != 'windows'
        run: chmod +x dist/CloudflareSpeedTest-${{ matrix.platform }}-${{ matrix.arch }}

      # 验证产物
      - name: 验证 arm64-musl 产物静态链接
        if: matrix.arch == 'arm64-musl'
        run: |
          sudo apt-get install -y file
          file dist/CloudflareSpeedTest-${{ matrix.platform }}-${{ matrix.arch }}
          aarch64-linux-musl-objdump -p dist/CloudflareSpeedTest-${{ matrix.platform }}-${{ matrix.arch }} | grep NEEDED

      # 上传产物
      - name: 上传构建产物
        uses: actions/upload-artifact@v4
        with:
          name: CloudflareSpeedTest-${{ matrix.platform }}-${{ matrix.arch }}
          path: dist/CloudflareSpeedTest-${{ matrix.platform }}-${{ matrix.arch }}${{ matrix.ext }}
          retention-days: 30

  # 创建 Release
  release:
    name: Create Release
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write
      packages: write
    
    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 下载所有构建产物
        uses: actions/download-artifact@v4
        with:
          path: ./artifacts

      - name: 显示下载的文件
        run: ls -R ./artifacts

      - name: 创建 Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            artifacts/CloudflareSpeedTest-windows-amd64/CloudflareSpeedTest-windows-amd64.exe
            artifacts/CloudflareSpeedTest-linux-amd64/CloudflareSpeedTest-linux-amd64
            artifacts/CloudflareSpeedTest-linux-arm64/CloudflareSpeedTest-linux-arm64
            artifacts/CloudflareSpeedTest-linux-arm64-musl/CloudflareSpeedTest-linux-arm64-musl
            artifacts/CloudflareSpeedTest-macos-amd64/CloudflareSpeedTest-macos-amd64
            artifacts/CloudflareSpeedTest-macos-arm64/CloudflareSpeedTest-macos-arm64
          draft: false
          prerelease: false
          generate_release_notes: true
          body: |
            ## 🚀 Cloudflare SpeedTest 跨平台版本发布
            
            ### 📦 可执行文件
            - **Windows (x64)**: CloudflareSpeedTest-windows-amd64.exe
            - **Linux (x64)**: CloudflareSpeedTest-linux-amd64
            - **Linux (ARM64 glibc)**: CloudflareSpeedTest-linux-arm64
            - **Linux (ARM64 musl)**: CloudflareSpeedTest-linux-arm64-musl（OpenWrt/ImmortalWrt 适配）
            - **macOS (Intel)**: CloudflareSpeedTest-macos-amd64
            - **macOS (Apple Silicon)**: CloudflareSpeedTest-macos-arm64
            
            ### 🌟 功能特性
            - 🎯 内置 97 个全球数据中心机场码
            - 🌍 支持城市名称智能识别（如"香港"、"东京"）
            - ⚙️ 预设配置：快速/标准/高质量测试
            - 🔧 完全自定义参数
            - 📊 自动生成测速报告
            - 🆕 小白快速测试：专为新手设计，只需输入3个数字
            - 📱 OpenWrt 适配：arm64-musl 版本静态链接，无额外依赖
            
            ### 🚀 使用方法
            1. 下载对应平台的可执行文件
            2. 双击运行（Windows）或赋予执行权限（Linux/macOS/OpenWrt）
            3. 按提示选择机场码和测试配置
            4. 等待测速完成，查看 result.csv 结果文件
            
            ### 📌 OpenWrt 特别说明
            - 适用架构：aarch64（`uname -m` 输出 aarch64）
            - 安装命令：
              ```bash
              # 上传文件到路由器
              scp CloudflareSpeedTest-linux-arm64-musl root@路由器IP:/tmp/
              # 赋予权限并运行
              chmod +x /tmp/CloudflareSpeedTest-linux-arm64-musl
              /tmp/CloudflareSpeedTest-linux-arm64-musl
